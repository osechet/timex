language: go

go:
- 1.11.x
- 1.12.x

script:
  - go fmt $(go list ./... | grep -v /vendor/)
  - go vet $(go list ./... | grep -v /vendor/)
  - go test -v -race -cover $(go list ./... | grep -v /vendor/)
